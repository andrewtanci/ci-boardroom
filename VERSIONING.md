# Versioning Standing Rule

Every future edit to CI Boardroom must:

1. bump the version line at the top of `BOARDROOM.md`;
2. add the corresponding entry to `CHANGELOG.md`;
3. prepare or update release notes as a draft;
4. stop before publishing the GitHub Release unless Andrew gives explicit written approval.

Pasted copies do not auto-update. The repository version is authoritative.
